{
  "name": "react-native-smooth-pull-to-refresh",
  "version": "0.9.1",
  "description": "custom pull to refresh component for react-native",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "pretest": "rimraf dist && tslint -p . && tsc -p .",
    "build": "rimraf dist && tsc -p .",
    "test": "jest",
    "prepublish": "rimraf dist && tsc -p ."
  },
  "keywords": [
    "react-native",
    "pull-to-refresh",
    "animated",
    "recompose",
    "typescript"
  ],
  "author": {
    "name": "passpier",
    "email": "poo9810@gmail.com"
  },
  "license": "MIT",
  "peerDependencies": {
    "react": ">=16.2",
    "react-native": ">0.54",
    "recompose": ">=0.30",
    "styled-components": ">=3"
  },
  "devDependencies": {
    "@types/react": "^16.7.22",
    "@types/react-native": "^0.57.32",
    "@types/styled-components": "^3",
    "@types/recompose": "^0.30.2",
    "typescript": "^3.2.2",
    "styled-components": "^3.3.2",
    "react": "^16.2.0",
    "recompose": "^0.30.0",
    "rimraf": "^2.6.2",
    "prettier": "^1.15.3"
  }
}
